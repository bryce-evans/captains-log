> Generated by planning/plan.py. Edit manually or re-run the planner to regenerate.

> **Issue tracking:** Tasks below are the static manifest. Live state lives in **bd (beads)** — each `Tnnn` ID maps to a bd issue via `.beads_map.json`. Do not edit Status fields here directly; instead use `bd update <id> --claim` to start work and `bd close <id>` to finish it. `render.py` reads live state from `bd show --json` and overlays it onto this manifest for the flowchart view. See [CLAUDE.md](CLAUDE.md) for the full agent workflow.

# Tasks

## Summary

**Total:** 43  |  **P0:** 14  |  **P1:** 23  |  **P2:** 6  |  **Human steps:** 5  |  **Complete:** 0

## Index

| ID | Workstream | Task | Crit | Est | Status |
|----|-----------|------|------|-----|--------|
| T001 | WS1 | Initialize Expo project with TypeScript and install core dependencies (expo-sqlite, zustand, expo-router, react-native-paper) | P0 | 2h | todo |
| T002 | WS1 | Define SQLite schema for schemas table, records table, and record_fields table; write migration runner | P0 | 4h | todo |
| T003 | WS1 | Implement SchemaRepository: CRUD for JSON schema definitions with two hardcoded MVP schemas pre-seeded | P0 | 4h | todo |
| T004 | WS1 | Implement RecordRepository: insert, fetch-all, fetch-by-schema, and attach-photo path methods | P0 | 4h | todo |
| T005 | WS1 | Expose Zustand store slices for activeSchema and recordDraft so WS2 and WS3 can read/write shared state | P0 | 2h | todo |
| T006 | WS1 | Build AI query engine: accept natural-language question, call GPT-4o to generate SQLite query, execute it, return formatted answer | P1 | 1d | todo |
| T007 | WS1 | Add auto-fill service: capture timestamp, GPS via expo-location, and weather from OpenWeatherMap at record-creation time | P1 | 4h | todo |
| T008 | WS1 | Write integration tests covering schema seed, record insert/retrieve, and a sample AI query round-trip | P2 | 4h | todo |
| T009 | WS2 | Integrate whisper.cpp via whisper.rn (or equivalent RN binding); bundle a quantized model file and verify on-device inference on iOS and Android | P0 | 1d | todo |
| T010 | WS2 | Implement microphone capture pipeline via Expo Audio: chunked recording suitable for feeding into whisper.cpp | P0 | 4h | todo |
| T011 | WS2 | Build on-device transcription handler that runs whisper.cpp on captured audio chunks and emits partial and final transcript events | P0 | 4h | todo |
| T012 | WS2 | Implement field-extraction engine: parse transcript against active schema using GPT-4o function-calling | P0 | 1d | todo |
| T013 | WS2 | Emit live field-update events to Zustand store as fields resolve during speech | P1 | 4h | todo |
| T014 | WS2 | Detect 'done' utterance and trigger record-assembly finalization | P1 | 2h | todo |
| T015 | WS2 | Assemble completed record object from resolved fields and hand off to WS1 persistence layer | P1 | 2h | todo |
| T016 | WS2 | Build query-mode voice session: whisper.cpp transcribes question, send transcript to GPT-4o for SQL, speak answer via on-device TTS | P1 | 1d | todo |
| T017 | WS2 | Handle mic permissions, background interruptions, and session cleanup gracefully | P1 | 4h | todo |
| T018 | WS3 | Set up Expo Router navigation shell with tab layout (Record, Albums, Query tabs) | P0 | 2h | todo |
| T019 | WS3 | Implement schema selector screen with two hardcoded MVP schemas and Zustand active-schema state | P0 | 4h | todo |
| T020 | WS3 | Build live checklist UI component consuming Zustand field-completion state from WS2 | P1 | 4h | todo |
| T021 | WS3 | Wire record creation screen: big button triggers WS2 voice session, renders live checklist, handles done signal | P1 | 1d | todo |
| T022 | WS3 | Build albums screen: list all records from WS1 SQLite, display fields in book-like card layout | P1 | 4h | todo |
| T023 | WS3 | Implement record detail view with field values and attached photo display | P1 | 4h | todo |
| T024 | WS3 | Add camera/gallery attachment via expo-image-picker on record creation screen | P1 | 2h | todo |
| T025 | WS3 | Build query screen: voice or text input, display WS1 AI answer in readable format | P1 | 4h | todo |
| T026 | WS3 | Integrate all workstreams and smoke-test full MVP demo | P0 | 4h | todo |
| T027 | WS2 | Spike: verify whisper.rn (or alt RN binding) viable in Expo dev client; benchmark candidate models on real device | P0 | 1d | todo |
| T028 | WS1 | Set up Expo dev client and EAS Build pipeline (iOS + Android local builds) | P0 | 4h | todo |
| T029 | WS1 | Decide and implement OpenAI key protection strategy before any non-dev distribution | P1 | 1d | todo |
| T030 | WS3 | End-of-record review modal: prompt before save when an important field is empty | P1 | 4h | todo |
| T031 | WS1 | Schema duplication on edit-attempt: edit creates a new schema instead of mutating | P2 | 4h | todo |
| T032 | WS2 | Type-coerce + validate GPT-4o-extracted field values against the active schema before storing | P1 | 4h | todo |
| T033 | WS4 | Establish testing infrastructure: Jest + RNTL + initial test coverage | P1 | 4h | todo |
| T034 | WS4 | GitHub Actions CI: typecheck + lint + test on every PR | P2 | 2h | todo |
| T035 | WS1 | Implement FileStorageService: paths, naming, and cleanup for audio and photos | P1 | 2h | todo |
| T036 | WS3 | Offline / loading / error UI states across record creation and query | P1 | 4h | todo |
| T037 | WS3 | App icon, splash screen, bundle id, app name | P2 | 2h | todo |
| T038 | WS4 | Write project README with setup, env-var, and dev-build instructions | P1 | 2h | todo |
| T039 | WS3 | Install Fraunces + Inter via expo-google-fonts; build typography preset components | P1 | 2h | todo |
| T040 | WS3 | Implement design tokens (color, type, spacing, radius, shadow) and the Surface primitive (paper bg + grain + top haze) | P1 | 4h | todo |
| T041 | WS3 | Motion utilities: reanimated spring presets, useBreathing hook, ripple primitive, prefers-reduced-motion bridge | P1 | 2h | todo |
| T042 | WS3 | Component primitives: FieldCard, BigButton (with ripples), PillSelector, ToastQuiet | P1 | 1d | todo |
| T043 | WS3 | Per-schema decorative SVG glyphs (wave for Fishing, sun for Art Show) | P2 | 2h | todo |

## Task Details

### T001 · Initialize Expo project with TypeScript and install core dependencies (expo-sqlite, zustand, expo-router, react-native-paper)

**Workstream:** WS1 — Bedrock  
**Criticality:** P0  
**Estimate:** 2h  
**Status:** todo  
**Depends on:**   
**Unlocks:** T002, T027, T028, T037, T038, T018, T019  
**Human required:**   
**Acceptance criteria:** Expo project initialized with TypeScript template; `tsconfig.json` has `strict: true`; expo-sqlite, zustand, expo-router, react-native-paper installed at versions compatible with Expo SDK 51+; `npm start` boots to a "Hello World" screen on iOS sim and Android emulator; `tsc --noEmit` passes.  
**Verification steps:** 1) Run `npm start`. 2) Open in iOS sim. 3) Open in Android emulator. 4) Run `tsc --noEmit`. 5) Confirm `expo-router` is the navigation root.  
**Verification tricky spots:** react-native-paper version must match the installed React Native version. expo-router requires a specific entrypoint (`app/` directory or `expo-router/entry`).  
**Notes:**   
**Assignee:**   

### T002 · Define SQLite schema for schemas table, records table, and record_fields table; write migration runner

**Workstream:** WS1 — Bedrock  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T003, T004  
**Human required:**   
**Acceptance criteria:** Tables created per ARCHITECTURE.md: `schemas(id, name, definition_json, created_at)`, `records(id, schema_id FK CASCADE, created_at, audio_path, photo_paths_json, fields_json)`. Indexes on `records.schema_id` and `records.created_at`. Migration runner reads a versioned migration list and applies pending ones idempotently on app boot. `PRAGMA foreign_keys = ON` is set per connection.  
**Verification steps:** 1) Boot app fresh — verify migrations run and tables exist. 2) Boot again — verify migrations are skipped (idempotent). 3) Insert a schema row, then a record referencing it, then delete the schema → verify cascade deletes the record. 4) Confirm indexes exist via `PRAGMA index_list`.  
**Verification tricky spots:** expo-sqlite enforces foreign keys only when explicitly enabled per connection. Migrations must run before any other DB access on app start. Avoid running migrations on every render — gate behind a one-time bootstrap.  
**Notes:** Uses JSON columns instead of normalized field tables to keep schema-per-record flexible. Trade-off: no FK constraints on field values.  
**Assignee:**   

### T003 · Implement SchemaRepository: CRUD for JSON schema definitions with two hardcoded MVP schemas pre-seeded

**Workstream:** WS1 — Bedrock  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T002  
**Unlocks:** T005, T031  
**Human required:**   
**Acceptance criteria:** `SchemaRepository` exposes `findAll()`, `findById(id)`, `create(definition)`, `delete(id)` per ARCHITECTURE.md. Two MVP schemas pre-seeded on first boot only (idempotent): "Fishing" (species, length_in, weight_lb, location, notes; species + length_in marked important) and "Art Show Sale" (item, price, buyer_name, payment_method, notes; item + price important). Schema definition JSON includes per-field `type` (string|number|enum), `important` (bool), and optional `enum_values`.  
**Verification steps:** 1) Fresh install → verify two schemas exist. 2) Re-launch → verify still only two (no duplication). 3) `create()` a third → `findAll()` returns three. 4) `delete()` schema with records → cascade deletes records (verifies T002 FK).  
**Verification tricky spots:** Pre-seeding must check existence by `id` (deterministic IDs like `mvp.fishing`), not by name, to remain idempotent.  
**Notes:** Schema editing is intentionally not supported — see PROJECT.md gap response. Edit-attempt behavior lives in T031 (post-MVP).  
**Assignee:**   

### T004 · Implement RecordRepository: insert, fetch-all, fetch-by-schema, and attach-photo path methods

**Workstream:** WS1 — Bedrock  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T002  
**Unlocks:** T005, T006, T007, T022, T023, T024  
**Human required:**   
**Acceptance criteria:** `RecordRepository` exposes `insert(schemaId, fields, photoPaths?, audioPath?)`, `findAll()`, `findBySchema(schemaId)`, `findById(id)`, `attachPhoto(recordId, path)`. Returns immutable record objects (per coding-style rules). Field values are validated against the referenced schema's types before insert; mismatched types reject with a typed error (caller decides UI behavior). Created/queried records preserve insertion order via `created_at DESC` default sort.  
**Verification steps:** 1) Insert a record with valid fields → `findById` returns the same shape. 2) Insert with mismatched type (e.g. "ABC" for number) → throws `SchemaValidationError`. 3) `findBySchema` filters correctly when both schemas have records. 4) `attachPhoto` appends without overwriting existing paths.  
**Verification tricky spots:** JSON serialization of `fields` must round-trip number types (avoid stringifying numbers). `photo_paths_json` must be an array even when only one photo. Concurrent inserts on app start could race the migration — gate behind bootstrap promise.  
**Notes:** Validation logic should be shared with T032 (extraction-time validation) — extract a `validateAgainstSchema()` helper.  
**Assignee:**   

### T005 · Expose Zustand store slices for activeSchema, recordDraft, and transcript so WS2 and WS3 can read/write shared state

**Workstream:** WS1 — Bedrock  
**Criticality:** P0  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T003, T004  
**Unlocks:** T012, T015, T021  
**Human required:**   
**Acceptance criteria:** Three slices implemented per ARCHITECTURE.md "Stores (Zustand contract)": `activeSchemaSlice` (`activeSchema`, `setActiveSchema`), `recordDraftSlice` (`draft`, `setField`, `clearDraft`, `markDone`), `transcriptSlice` (`partial`, `final[]`, `appendPartial`, `commitFinal`, `clear`). All slices exported from a single `useStore` hook. `markDone()` calls RecordRepository.insert and clears the draft on success. Types are exported so WS2 and WS3 can consume them without duplication.  
**Verification steps:** 1) Set active schema → reads from another component reflect change. 2) `setField('species', 'perch')` → `draft.species === 'perch'`. 3) `markDone()` with valid draft → SQLite row created, draft cleared. 4) `markDone()` with empty important fields → returns a typed warning (consumed by T030 modal).  
**Verification tricky spots:** Zustand subscriptions can over-render — use selectors. `markDone` is async; ensure UI doesn't double-fire on tap.  
**Notes:** `transcriptSlice` is owned by WS1 (slice definition) but written-to by WS2 (T013) and read-from by WS3 (T020). This is the single shared contract surface across workstreams.  
**Assignee:**   

### T006 · Build AI query engine: accept natural-language question, call GPT-4o to generate SQLite query, execute it, return formatted answer

**Workstream:** WS1 — Bedrock  
**Criticality:** P1  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T004  
**Unlocks:** T008, T016, T025  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T007 · Add auto-fill service: capture timestamp, GPS via expo-location, and weather from OpenWeatherMap at record-creation time

**Workstream:** WS1 — Bedrock  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T004  
**Unlocks:** T021  
> **Human required:** Register at openweathermap.org → create a free API key → add to .env as OPENWEATHER_API_KEY.  
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T008 · Write integration tests covering schema seed, record insert/retrieve, and a sample AI query round-trip

**Workstream:** WS1 — Bedrock  
**Criticality:** P2  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T006  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T009 · Integrate whisper.cpp via whisper.rn (or equivalent RN binding); bundle a quantized model file and verify on-device inference on iOS and Android

**Workstream:** WS2 — Songbird  
**Criticality:** P0  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T011  
> **Human required:** Download a whisper.cpp ggml model file (recommended: ggml-base.en.q5_1) from https://huggingface.co/ggerganov/whisper.cpp and place it in the app's assets directory per whisper.rn's bundling instructions. May also require switching the project from Expo Go to a custom dev client because whisper.rn ships native code.  
**Acceptance criteria:** whisper.rn (or chosen binding) installed and configured; bundled model loads at app start without crashing; a hardcoded short audio file can be transcribed end-to-end on both iOS and Android with reasonable latency (target: < 1s for a 3s clip on a recent device); no network calls during transcription (verified via Charles/Proxyman or equivalent).  
**Verification steps:** 1) Run app on iOS simulator and a real Android device. 2) Trigger transcription of a bundled test WAV file. 3) Confirm transcript text returns and matches expected output. 4) Run with airplane mode enabled to confirm offline operation.  
**Verification tricky spots:** Expo Go does not support native modules — switching to a dev client breaks `expo start` workflow. Model file must be loaded from a path the native code can read; iOS bundle paths and Android assets behave differently. First-launch model load can take several seconds and may need a splash/loader UI.  
**Notes:** Replaces the previously-planned OpenAI Realtime API integration. See PROJECT.md updates dated 2026-04-27 for context.  
**Assignee:**   

### T010 · Implement microphone capture pipeline via Expo Audio: chunked recording suitable for feeding into whisper.cpp

**Workstream:** WS2 — Songbird  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T028, T035  
**Unlocks:** T011, T017  
**Human required:**   
**Acceptance criteria:** Microphone capture starts and stops cleanly; audio is recorded in a format whisper.cpp accepts (16kHz mono PCM/WAV); audio is delivered to the transcription layer in chunks short enough to feel "live" (target: 1-3s segments) without dropping samples between chunks.  
**Verification steps:** 1) Tap a "Record" button, speak, tap stop. 2) Verify resulting audio file plays back correctly. 3) Verify chunk boundaries do not lose audio. 4) Confirm the captured format is directly consumable by whisper.cpp without additional resampling.  
**Verification tricky spots:** Expo Audio defaults may not match whisper.cpp's expected sample rate (16kHz mono); explicit format configuration is required. Background-mode behavior and audio-session category interact with chunked recording.  
**Notes:** No more streaming-over-WebSocket pipeline (that requirement was removed when transcription moved on-device). This task is now scoped to local capture only.  
**Assignee:**   

### T011 · Build on-device transcription handler that runs whisper.cpp on captured audio chunks and emits partial and final transcript events

**Workstream:** WS2 — Songbird  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T009, T010  
**Unlocks:** T012  
**Human required:**   
**Acceptance criteria:** A handler accepts audio chunks from T010, runs whisper.cpp inference, and emits typed events: `partial` (rolling transcript while user is still speaking) and `final` (complete utterance after silence/done detection). No network traffic during transcription. End-to-end latency from end-of-utterance to `final` event is acceptable on a real mid-tier device (target: < 500ms for short utterances).  
**Verification steps:** 1) Speak a known sentence into the mic. 2) Verify `partial` events fire with progressively-completing text. 3) Verify exactly one `final` event fires when the utterance ends. 4) Run in airplane mode and confirm transcripts still appear.  
**Verification tricky spots:** Inference is CPU-bound and can block the JS thread if not run on a worker/native thread. whisper.cpp's streaming-mode behavior depends on the binding; partial events may be approximations rather than true streaming output. Silence detection (where to cut "final" boundaries) is a separate concern from transcription itself.  
**Notes:** Originally planned to consume Realtime API events; now consumes T010 audio chunks directly and runs whisper.cpp from T009.  
**Assignee:**   

### T012 · Implement field-extraction engine: parse transcript against active schema using GPT-4o function-calling

**Workstream:** WS2 — Songbird  
**Criticality:** P0  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T005, T011  
**Unlocks:** T013, T014, T032  
> **Human required:** Create an OpenAI API key at platform.openai.com/api-keys → add to `.env` as `OPENAI_API_KEY`. Used by T012 (field extraction) and T006/T016 (SQL generation). For dev MVP only — distribution requires a backend proxy (see T029 and PROJECT.md gap notes).  
**Acceptance criteria:** Given a `final` transcript and the active schema, calls GPT-4o with a function-call definition derived from the schema's field list and types. Returns a partial `Record<FieldKey, FieldValue>` containing only fields the user mentioned (no hallucinated values, no overwriting fields not present in the utterance). Result is passed to T032 for type-coerce + validation before being merged into `recordDraftSlice`. Handles GPT-4o errors (timeout, rate limit, malformed JSON) with typed errors that callers can display.  
**Verification steps:** 1) Transcript "I caught a 12 inch perch" against fishing schema → returns `{species: "perch", length_in: 12}` only. 2) Empty transcript → returns `{}`. 3) Transcript referring to a non-existent field → that field is dropped, not added. 4) Network failure → throws `ExtractionNetworkError`. 5) Verify no fields outside the schema are present in the result.  
**Verification tricky spots:** GPT-4o can return "null" or "unknown" string for missing values — must be filtered, not coerced. Function-call schema generation must match the active schema dynamically; cache by `schema.id`. Cost: each utterance is one GPT-4o call — keep prompt minimal.  
**Notes:**   
**Assignee:**   

### T013 · Emit live field-update events to Zustand store as fields resolve during speech

**Workstream:** WS2 — Songbird  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T012, T032  
**Unlocks:** T015, T020  
**Human required:**   
**Acceptance criteria:** Each finalized utterance flows: T011 final → T012 extract → T032 validate → `recordDraftSlice.setField` for each valid field. Invalid fields surface as a transient warning (consumed by T036 UI). Multiple utterances for the same field overwrite (latest wins). Setting a field while a record is being saved is rejected.  
**Verification steps:** 1) Speak "perch", then "12 inches" — `draft` ends with `{species: "perch", length_in: 12}`. 2) Speak "purple" for `length_in` — value rejected, warning emitted, prior value preserved. 3) Speak "10 inches" then "12 inches" — final value is 12.  
**Verification tricky spots:** Zustand updates that lose subsequent updates if callers don't use the functional setter. Race between user pressing "done" and a still-pending GPT-4o response — finalize should drain in-flight extractions or cancel them.  
**Notes:** Writes only to slices defined in T005 — no direct DB calls.  
**Assignee:**   

### T014 · Detect 'done' utterance and trigger record-assembly finalization

**Workstream:** WS2 — Songbird  
**Criticality:** P1  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T012  
**Unlocks:** T015  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T015 · Assemble completed record object from resolved fields and hand off to WS1 persistence layer

**Workstream:** WS2 — Songbird  
**Criticality:** P1  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T013, T014, T005  
**Unlocks:** T021  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T016 · Build query-mode voice session: whisper.cpp transcribes question, send transcript to GPT-4o for SQL, speak answer via on-device TTS

**Workstream:** WS2 — Songbird  
**Criticality:** P1  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T006, T011  
**Unlocks:** T025  
**Human required:**   
**Acceptance criteria:** End-to-end voice query works: user taps mic → speaks question → whisper.cpp produces transcript → GPT-4o generates SQL → query runs against local SQLite → answer is rendered as text and spoken via on-device TTS.  
**Verification steps:** 1) Pre-seed a few records. 2) Ask "how many records do I have" verbally. 3) Verify a correct answer is both displayed and spoken. 4) Repeat with a richer query (e.g. biggest fish). 5) Verify transcription happens without network (airplane mode confirms whisper step is local; only the GPT-4o call hits the network).  
**Verification tricky spots:** TTS quality varies a lot between iOS and Android system voices. expo-speech has occasional hangs on Android if called before audio session is ready; sequence with mic teardown matters. The SQL-generation prompt needs the schema definition embedded so GPT-4o can produce valid SQLite.  
**Notes:** Originally used OpenAI Realtime for the whole loop including TTS. Now: whisper.cpp for STT, GPT-4o for SQL only, expo-speech for TTS. Three separate components instead of one streaming session.  
**Assignee:**   

### T017 · Handle mic permissions, background interruptions, and session cleanup gracefully

**Workstream:** WS2 — Songbird  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T010  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T018 · Set up Expo Router navigation shell with tab layout (Record, Albums, Query tabs)

**Workstream:** WS3 — Folio  
**Criticality:** P0  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T019, T020, T021, T022, T023, T024, T025, T036  
**Human required:**   
**Acceptance criteria:** Three tabs render and navigate: Record (default), Albums, Query. Tab bar uses react-native-paper styling. Deep-linkable routes via expo-router file-based routing (`app/(tabs)/record.tsx`, etc.). Schema selector accessible from Record tab header (icon → modal/route). Theme provider wraps the navigator.  
**Verification steps:** 1) App opens to Record tab. 2) Tap each tab → route changes. 3) Hard-reload preserves tab. 4) Schema selector opens from Record tab. 5) Verify safe-area handling on iOS notch and Android nav bar.  
**Verification tricky spots:** expo-router and react-native-paper theming can fight over status-bar style. Tab icons must be loaded from a local icon set or vector library (lucide-react-native or @expo/vector-icons).  
**Notes:**   
**Assignee:**   

### T019 · Implement schema selector screen with two hardcoded MVP schemas and Zustand active-schema state

**Workstream:** WS3 — Folio  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T003, T005, T018, T042  
**Soft depends on:** T043 (decorative glyphs — selector functional without them)  
**Unlocks:** T021  
**Human required:**   
**Acceptance criteria:** Screen lists schemas from `SchemaRepository.findAll()` (not hardcoded — repository is the source). Tapping a row sets `activeSchema` via `setActiveSchema()`. Active row is visually marked. Persists across app restarts (active-schema id stored in async-storage or sqlite). Empty-state copy if for some reason no schemas exist (defensive — should never happen post-T003 seeding).  
**Verification steps:** 1) Cold launch → "Fishing" or "Art Show Sale" is selected (deterministic default = first by name). 2) Tap the other → it becomes active. 3) Kill and relaunch → previously-active schema is still active. 4) Switch tabs and back → selection persists in memory.  
**Verification tricky spots:** Persisting active-schema id needs to happen synchronously enough that the Record screen doesn't render "no schema selected" on cold launch.  
**Notes:**   
**Assignee:**   

### T020 · Build live checklist UI component consuming Zustand field-completion state from WS2

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T013, T018, T042  
**Unlocks:** T021  
**Human required:**   
**Acceptance criteria:** `LiveChecklist` composes `FieldCard` primitives per DESIGN.md "Record" screen spec. Reads `activeSchema` and `recordDraft` from Zustand. Empty fields stack at top with important-field wheat stripe; filled fields slide below a hairline divider with the "filled" caption (`gentle` spring). Listening-active dot pulses on the field currently being captured. Validation rejection (T032) flashes `rust` 6% wash for 280ms. Honors `prefersReducedMotion` from T041.  
**Verification steps:** 1) Two important + one optional field — important fields render at top with wheat stripe. 2) Set a field via store → field reorders to filled section with smooth spring. 3) Trigger a validation rejection → rust wash appears and clears. 4) Toggle reduced motion → reorder becomes a quick fade.  
**Verification tricky spots:** FlatList vs Reanimated layout animations — use `Layout.springify()` from reanimated for reorder. Avoid re-renders on every transcript partial; subscribe only to `recordDraft`.  
**Notes:** All visual treatment lives in the FieldCard primitive (T042). This task wires data → primitive composition only.  
**Assignee:**   

### T021 · Wire record creation screen: big button triggers WS2 voice session, renders live checklist, handles done signal

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T005, T015, T019, T020, T030, T042  
**Soft depends on:** T007 (auto-fill — record creation works without weather; gracefully no-ops if T007 not yet integrated)  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:** Big record button fills the screen. Tap → starts T011 transcription session and shows live checklist (T020). Important fields appear at top until filled, then move to bottom. Tap stop or detect "done" utterance → fires T030 review modal if any important field is empty; otherwise saves via `markDone()`. Auto-fill (T007) populates GPS/weather/timestamp at session start if available; failures don't block save. Photo attach button (T024) opens picker.  
**Verification steps:** 1) Tap record, speak all fields, say "done" → record persists. 2) Speak only some important fields, say "done" → modal appears. 3) Disable network → record still saves (extraction errors surface but capture works). 4) Cancel mid-session → no record saved.  
**Verification tricky spots:** Race between final transcription chunk and "done" signal — finalization must drain pending extractions. Long-press vs tap semantics on the record button.  
**Notes:** T007 is intentionally a soft dep; integrate it when available, no-op cleanly when not.  
**Assignee:**   

### T022 · Build albums screen: list all records from WS1 SQLite, display fields in book-like card layout

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T004, T018  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T023 · Implement record detail view with field values and attached photo display

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T004, T018  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T024 · Add camera/gallery attachment via expo-image-picker on record creation screen

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T004, T018, T035  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T025 · Build query screen: voice or text input, display WS1 AI answer in readable format

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T006, T016, T018  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:**   
**Verification steps:**   
**Verification tricky spots:**   
**Notes:**   
**Assignee:**   

### T026 · Integrate all workstreams and smoke-test full MVP demo

**Workstream:** WS3 — Folio  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T008, T017, T021, T022, T023, T024, T025, T036  
**Unlocks:**   
**Human required:**   
**Acceptance criteria:** End-to-end demo passes on a real device. Demo script: (1) cold launch → schema selector shows two schemas; (2) select Fishing → land on Record tab; (3) tap record, say "I caught a 14 inch perch off the dock"; checklist updates live, length_in and species fill, location fills; (4) attach a photo; (5) say "done"; (6) record appears in Albums tab as a card with all fields and photo; (7) open Query tab, ask "what was my biggest fish?"; (8) hear and see correct answer. Airplane-mode subset: steps 1, 2, 3 must work; 4 works; 5 saves locally; 6 displays; 7 shows offline error gracefully.  
**Verification steps:** Run the full script above on iOS sim, an iOS device, an Android device. Record a video as evidence. Capture screenshots for README.  
**Verification tricky spots:** Real-device first-launch model load (T009) can be slow; budget time for that. Photo attach + audio + extraction together is the most likely place for memory pressure on lower-end Android.  
**Notes:** This is the gate to call MVP "done". Failures here generate bd issues, do not block-merge work directly.  
**Assignee:**   

### T027 · Spike: verify whisper.rn (or alternative RN binding) is viable in an Expo dev client; benchmark candidate models on a real device

**Workstream:** WS2 — Songbird  
**Criticality:** P0  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T001, T028  
**Unlocks:** T009  
**Human required:**   
**Acceptance criteria:** Time-boxed feasibility study. Outputs: (1) decision doc committed to repo recommending a specific binding (whisper.rn vs alternative) with maintenance status, Expo compatibility, build complexity; (2) benchmark table of at least three model variants (e.g. tiny.en.q5_1, base.en.q5_1, small.en.q5_1) measuring app-bundle size, model-load time on cold start, transcription latency for a 3s clip, and qualitative accuracy on 5 fishing-domain test phrases; (3) recommended model for MVP with rationale. If no binding is viable, document the blocker and propose alternatives (e.g. native module wrapper, switch to cloud Whisper).  
**Verification steps:** 1) Decision doc reviewed and accepted. 2) Benchmark numbers reproducible on the test device. 3) Recommended model and binding usable as input to T009 acceptance criteria.  
**Verification tricky spots:** Real-device benchmarks differ wildly between simulators and physical hardware — only physical-device numbers count. Don't skip Android — its CPU envelope is the binding constraint.  
**Notes:** Created from planner audit 2026-04-27. Must precede T009 because the decision determines T009's acceptance criteria.  
**Assignee:**   

### T028 · Set up Expo dev client and EAS Build pipeline (iOS + Android local builds)

**Workstream:** WS1 — Bedrock  
**Criticality:** P0  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T009, T010, T027  
**Human required:**   
**Acceptance criteria:** `eas.json` committed with at least `development`, `preview`, and `production` profiles. `expo-dev-client` installed and configured. Dev client builds locally for iOS sim and Android device using `eas build --local` (no remote-build dependency for inner loop). `package.json` scripts: `npm run dev:ios`, `npm run dev:android`, `npm run build:dev`. Documented in README (T038) once that exists.  
**Verification steps:** 1) `eas build --profile development --platform ios --local` produces a runnable .app. 2) Same for Android (.apk). 3) Custom dev-client opens and connects to the Metro bundler. 4) Adding a native module (e.g. whisper.rn in T009) and rebuilding works without manual native edits.  
**Verification tricky spots:** EAS Build local mode requires Xcode + Android SDK toolchains installed locally. iOS code-signing for sim builds is automatic but device builds need a free Apple developer account. Android keystore for dev profile can be auto-generated.  
**Notes:** Created from planner audit 2026-04-27. Blocks T009 because whisper.rn is a native module that Expo Go cannot run.  
**Assignee:**   

### T029 · Decide and implement OpenAI key protection strategy before any non-dev distribution

**Workstream:** WS1 — Bedrock  
**Criticality:** P1  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T012  
**Unlocks:**   
> **Human required:** Decide between (a) Cloudflare Worker proxy, (b) Supabase Edge Function, (c) Vercel serverless function, or (d) defer behind a "do-not-distribute" feature flag. Whichever is chosen requires the human to register the account and provision the deployment.  
**Acceptance criteria:** Either: (a) a deployed proxy that the app uses for GPT-4o calls (no `OPENAI_API_KEY` in client), with rate limiting and a per-device token; OR (b) an explicit `IS_DISTRIBUTION_BUILD` guard that fails the app at boot if a non-dev build is run with a bundled key, plus a tracking issue for the proxy work. The decision and rationale committed to ARCHITECTURE.md.  
**Verification steps:** If proxy: 1) Build with no `OPENAI_API_KEY` in env → field extraction still works. 2) Inspect the binary → no key string present. 3) Direct request to OpenAI from the app fails (only proxy path works). If guard: 1) Distribution build with bundled key → app refuses to start with a clear error.  
**Verification tricky spots:** Expo public env (`EXPO_PUBLIC_*`) is bundled and extractable; a private `.env` value still ends up bundled if read at module top-level. Confirm via `unzip -p` of the .ipa/.apk.  
**Notes:** Created from planner audit 2026-04-27. PROJECT.md flags this as blocking before public distribution. Acceptable to ship dev-MVP with a bundled key as long as the build is guarded against accidental distribution.  
**Assignee:**   

### T030 · End-of-record review modal: prompt before save when an important field is empty

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T005, T015, T020  
**Unlocks:** T021  
**Human required:**   
**Acceptance criteria:** When the user signals "done" (utterance or button) and the active schema's important fields include any with empty values in the draft, render a modal listing those fields with three actions: "Save anyway" (proceeds via `markDone()`), "Add by voice" (resumes the voice session), "Cancel" (returns to record screen without saving). When all important fields are filled, save proceeds without showing the modal.  
**Verification steps:** 1) Schema with `species` important + `notes` not — leave species empty, say done → modal lists species. 2) Fill all important fields → modal does not appear. 3) "Save anyway" persists the record with empty important field. 4) "Add by voice" reopens the mic without losing the existing draft.  
**Verification tricky spots:** Modal must not block the JS thread while an in-flight extraction completes. "Add by voice" must not double-claim the audio session.  
**Notes:** Created from planner audit 2026-04-27. Implements PROJECT.md success criterion at line 15: "double check at the end to confirm with the user it is okay if [important fields] are empty".  
**Assignee:**   

### T031 · Schema duplication on edit-attempt: when user tries to edit an existing schema, create a new one instead

**Workstream:** WS1 — Bedrock  
**Criticality:** P2  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T003  
**Unlocks:**   
**Human required:**   
**Acceptance criteria:** Post-MVP. UX: user opens an existing schema in an "edit" view; on save, a new schema row is created with a new id (e.g. "Fishing v2") and the original is preserved. Records remain bound to their original schema. Confirmed UI copy explains the duplicate-on-edit behavior. Optional: schema list groups duplicates visually.  
**Verification steps:** 1) Edit Fishing → save → "Fishing v2" appears in list, original Fishing is unchanged. 2) Records under original Fishing still query correctly. 3) Records cannot be moved between schemas (out of scope).  
**Verification tricky spots:** Naming collision when "Fishing v2" already exists — auto-increment to v3.  
**Notes:** Created from planner audit 2026-04-27. Out of MVP scope per PROJECT.md priorities (only two predefined schemas in MVP). Filed for traceability.  
**Assignee:**   

### T032 · Type-coerce + validate GPT-4o-extracted field values against the active schema before storing

**Workstream:** WS2 — Songbird  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T004, T012  
**Unlocks:** T013  
**Human required:**   
**Acceptance criteria:** A `validateAgainstSchema(extracted, schema)` function (shared with T004's repo-level validation) that: (1) coerces strings → numbers where the schema declares numeric (e.g. "12" → 12, "twelve" → throws or rejects); (2) rejects values that cannot be coerced and returns a typed `ValidationError` per field; (3) for enum fields, normalizes to the canonical case-insensitive match or rejects; (4) drops fields not present in the schema entirely. Returns `{accepted: Record<FieldKey, FieldValue>, rejected: Record<FieldKey, ValidationError>}`. Pure function, no side effects.  
**Verification steps:** 1) `{length_in: "12"}` against numeric schema → accepted as `{length_in: 12}`. 2) `{length_in: "ABC"}` → rejected with reason. 3) `{species: "Perch"}` against enum `["perch","bass","trout"]` → accepted as `"perch"` (lowercased). 4) `{unknown_field: "x"}` → dropped silently. 5) Round-trip test: validated output passes RecordRepository.insert.  
**Verification tricky spots:** GPT-4o sometimes returns numbers as strings ("12") even when function-call schema declares number — coerce defensively. Negative-number edge cases. Don't reject empty strings as invalid numbers — treat as "not mentioned".  
**Notes:** Created from planner audit 2026-04-27. Implements PROJECT.md success criterion at line 29: "we can't store 'ABC' as the temperature".  
**Assignee:**   

### T033 · Establish testing infrastructure: Jest + React Native Testing Library + initial test coverage

**Workstream:** WS4 — Steward  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T008, T034  
**Human required:**   
**Acceptance criteria:** `jest`, `@testing-library/react-native`, `jest-expo` configured. `npm test` runs the suite. Initial unit tests in place for: SchemaRepository (T003), RecordRepository (T004), validateAgainstSchema (T032), at least one component (T020 LiveChecklist). Coverage threshold of 80% configured but not enforced until critical path is implemented (logged warning rather than CI failure during early development). Mock strategy documented for whisper.cpp and GPT-4o (test seams, not real network).  
**Verification steps:** 1) `npm test` runs and passes with the seed tests. 2) `npm run test:coverage` produces a report. 3) A deliberately broken test fails clearly. 4) Mocks for native modules (whisper.rn) load without crashing the test runner.  
**Verification tricky spots:** jest-expo + native modules requires explicit mocks in `jest.setup.js`. expo-sqlite is not trivially testable in jsdom — use an in-memory SQLite shim or stub at the repository boundary.  
**Notes:** Created from planner audit 2026-04-27. T008 (WS1 integration tests) builds on this scaffolding.  
**Assignee:**   

### T034 · GitHub Actions CI: typecheck + lint + test on every PR

**Workstream:** WS4 — Steward  
**Criticality:** P2  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T033  
**Unlocks:**   
**Human required:**   
**Acceptance criteria:** `.github/workflows/ci.yml` runs on PR and main pushes. Steps: install (with cache), `tsc --noEmit`, `eslint`, `jest --ci`. Fails the PR check on any failure. Required-status-checks configured on the default branch. EAS builds NOT run in CI (cost) — only static checks.  
**Verification steps:** 1) Open a draft PR → CI runs and passes. 2) Push a typecheck failure → CI fails with clear log. 3) Caching: second run is meaningfully faster than the first.  
**Verification tricky spots:** Node version must match local. Cache key should include `package-lock.json` hash. Concurrent PRs from the same actor — set `concurrency:` to cancel stale runs.  
**Notes:** Created from planner audit 2026-04-27.  
**Assignee:**   

### T035 · Implement FileStorageService: paths, naming, and cleanup for audio and photos

**Workstream:** WS1 — Bedrock  
**Criticality:** P1  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T010, T024  
**Human required:**   
**Acceptance criteria:** `FileStorageService` exposes `pathForAudio(recordId)`, `pathForPhoto(recordId, index)`, `delete(path)`, `cleanupOrphans()`. All paths anchored to `FileSystem.documentDirectory`. Layout: `documentDirectory/records/{recordId}/audio.wav`, `documentDirectory/records/{recordId}/photo_{index}.jpg`. `cleanupOrphans()` finds files without a matching record row and deletes them; runs on app boot in dev, on demand in prod. Audio files are deletable independently (can be discarded after successful extraction to reclaim space).  
**Verification steps:** 1) `pathForAudio('abc')` returns `<docDir>/records/abc/audio.wav`. 2) Create a record, then orphan a file → `cleanupOrphans()` removes it. 3) Delete a record → cascade cleans its files (called from RecordRepository.delete in a future task). 4) Path collisions across reinstalls handled (documentDirectory survives upgrades but not reinstalls — accept).  
**Verification tricky spots:** Android scoped storage rules. iOS sandbox paths differ between sim and device. expo-file-system has differing behavior between bare and managed workflows.  
**Notes:** Created from planner audit 2026-04-27. Audio retention policy: keep until extraction succeeds, then deletable. Photos retained for record lifetime.  
**Assignee:**   

### T036 · Offline / loading / error UI states across record creation and query

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T013, T020, T021, T025  
**Unlocks:** T026  
**Human required:**   
**Acceptance criteria:** Four states rendered consistently: (1) whisper warm-up (model loading on cold start) → loader on Record tab; (2) GPT-4o extraction in flight → subtle indicator on the affected field; (3) network failure during extraction → toast with "queued, will retry when online" plus actual retry on connectivity restore; (4) GPT-4o failure during query → readable error with retry button (not raw stack). Validation rejections from T032 surface as inline warnings on the field. All copy reviewed for accessibility (announces via screen reader).  
**Verification steps:** 1) Cold launch → see warm-up loader, then it disappears. 2) Airplane mode mid-record → toast appears, capture continues, queue drains on reconnect. 3) Force GPT-4o to fail → user sees actionable error, not a crash.  
**Verification tricky spots:** "Queued for retry" UX requires actually persisting the queue (sqlite or async-storage) so it survives app close. NetInfo listeners can fire spuriously — debounce.  
**Notes:** Created from planner audit 2026-04-27. PROJECT.md success criteria allow offline capture; this task implements that promise.  
**Assignee:**   

### T037 · App icon, splash screen, bundle id, app name

**Workstream:** WS3 — Folio  
**Criticality:** P2  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:**   
> **Human required:** Provide a 1024x1024 app icon source PNG and a splash background color/image. Decide on bundle identifier (e.g. `com.<your-handle>.captainslog`) and display name. iOS App Store and Android Play Store distribution will require additional metadata — out of scope for dev MVP.  
**Acceptance criteria:** `app.json` configured with name, slug, bundle id, icon, splash, supported orientations (portrait only for MVP), and platform-specific metadata. Generated icon set includes all required iOS and Android sizes (use `expo-asset` plugin or manual generator). Splash renders correctly on iOS notch and Android nav-bar devices.  
**Verification steps:** 1) Build dev client → home-screen icon is correct on iOS and Android. 2) App opens to splash before any JS code runs. 3) Splash transitions cleanly to first screen without flash-of-unstyled-content.  
**Verification tricky spots:** Adaptive icons on Android need a foreground + background. iOS splash deprecation in newer SDKs — use the `expo-splash-screen` config.  
**Notes:** Created from planner audit 2026-04-27. Distribution-ready assets are post-MVP.  
**Assignee:**   

### T038 · Write project README with setup, env-var, and dev-build instructions

**Workstream:** WS4 — Steward  
**Criticality:** P1  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T001, T028  
**Unlocks:**   
**Human required:**   
**Acceptance criteria:** Top-level `README.md` covers: (1) project description (one paragraph from PROJECT.md); (2) prereqs (Node version, Xcode, Android SDK, EAS account); (3) clone + install steps; (4) `.env` setup with required keys (`OPENAI_API_KEY`, `OPENWEATHER_API_KEY`) and where they come from; (5) whisper model download + placement (links to Hugging Face); (6) dev-client build commands for iOS + Android; (7) test + lint commands; (8) link to PLAN.md / TASKS.md / ARCHITECTURE.md / STYLE.md for deeper reading; (9) `.env.example` checked in.  
**Verification steps:** 1) A new contributor follows the README from a clean checkout and reaches a running dev client without asking questions. 2) `.env.example` matches the variables actually consumed in code. 3) Links resolve.  
**Verification tricky spots:** Keep it short — the canonical detail lives in ARCHITECTURE.md and TASKS.md. README is a runway.  
**Notes:** Created from planner audit 2026-04-27.  
**Assignee:**   

### T039 · Install Fraunces + Inter via expo-google-fonts; build typography preset components

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T040, T042  
**Human required:**   
**Acceptance criteria:** `@expo-google-fonts/fraunces` (variable, with SOFT axis) and `@expo-google-fonts/inter` (variable) installed and loaded at app boot via `useFonts` (splash screen held until ready). Preset typography components in `src/components/type/`: `<DisplayHero>`, `<DisplayLead>`, `<DisplayTitle>`, `<BodyLg>`, `<BodyMd>`, `<MicroCaps>`. Each preset reads from `type.size`, `type.line`, `type.track` tokens. Inter renders with `tnum` (tabular numerals) and `cv11` enabled per DESIGN.md. Fraunces uses `fontVariationSettings` to set the SOFT axis to 80.  
**Verification steps:** 1) App boots without flash-of-system-font. 2) `<DisplayHero>` renders Fraunces with soft terminals visible. 3) Numerals in `<BodyMd>` align in a column (tabular). 4) Toggling Dynamic Type on iOS scales all presets proportionally.  
**Verification tricky spots:** Variable-axis support varies between iOS and Android — `fontVariationSettings` on Android requires `fontFamily` to point to the variable file path, not the static weights. Splash-screen handoff with `expo-splash-screen` must be after fonts resolve, not before.  
**Notes:** Created from DESIGN.md 2026-04-27. All screen tasks should compose these presets, never raw `fontSize`.  
**Assignee:**   

### T040 · Implement design tokens (color, type, spacing, radius, shadow) and the Surface primitive

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 4h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T042  
**Human required:**   
**Acceptance criteria:** `src/styles/tokens.ts` exports the full token set per DESIGN.md "Design Tokens" section: `color`, `type`, `space`, `radius`, `elevation`, `motion`. All tokens typed as `as const` for exhaustive autocomplete. `Surface` component in `src/components/primitives/Surface.tsx` renders the screen background: `paper` color fill, paper-grain noise overlay (PNG asset under `assets/textures/grain.png` at 6–8% opacity, full-bleed `Image` with `repeat`), and a single soft radial gradient at top (ember 6% center fading to transparent at 50% screen height) via `expo-linear-gradient` or `react-native-svg`. Composes with `SafeAreaView` and exposes `children` for screen content.  
**Verification steps:** 1) Screen using `Surface` shows warm paper bg, not flat white. 2) Grain texture visible up close, invisible at glance. 3) Top haze reads as soft warmth, not as a "gradient hero." 4) Pixel-snapshot test on iOS sim and Android emulator matches a reference screenshot.  
**Verification tricky spots:** Noise overlay can ship as an SVG `<feTurbulence>` filter (smaller bundle) or pre-baked PNG (more reliable cross-platform). Pre-baked PNG recommended for RN. Radial gradients in RN require `react-native-svg` — `expo-linear-gradient` is linear-only.  
**Notes:** Created from DESIGN.md 2026-04-27. The Surface primitive is the only screen background — no screen sets its own bg color.  
**Assignee:**   

### T041 · Motion utilities: reanimated spring presets, useBreathing hook, ripple primitive, prefers-reduced-motion bridge

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T001  
**Unlocks:** T042  
**Human required:**   
**Acceptance criteria:** `react-native-reanimated` v3 installed and Babel plugin configured. `src/styles/motion.ts` exports the `gentle` / `buoyant` / `settle` spring configs from DESIGN.md. `useBreathing()` hook in `src/hooks/useBreathing.ts` returns a `SharedValue<number>` cycling 1.0 → 1.02 → 1.0 over 2400ms infinite, suitable for a `transform: [{ scale }]`. `Ripple` component in `src/components/primitives/Ripple.tsx` accepts `active: boolean` and emits concentric soft `emberSoft` rings every 1.6s while active (cleans up on unmount). `useReducedMotion()` hook bridges `AccessibilityInfo.isReduceMotionEnabled` and listens for changes. All motion utilities call `withSpring` only when reduced motion is OFF; collapse to `withTiming(180)` when ON.  
**Verification steps:** 1) Sample component using `useBreathing` visibly breathes at 2.4s cycle. 2) Toggling iOS Settings → Accessibility → Reduce Motion pauses the breathing within one cycle. 3) `Ripple` component cleans up when `active=false` (no leaked animations). 4) Springs feel underdamped — slight overshoot — not snappy.  
**Verification tricky spots:** Reanimated v3 worklets can't read regular React state — pass reduced-motion as a `SharedValue`. `AccessibilityInfo` listener must be removed on unmount. Ripple emission interval drifts if not anchored to absolute time.  
**Notes:** Created from DESIGN.md 2026-04-27. Reduced motion is non-negotiable — design explicitly requires it.  
**Assignee:**   

### T042 · Component primitives: FieldCard, BigButton (with ripples), PillSelector, ToastQuiet

**Workstream:** WS3 — Folio  
**Criticality:** P1  
**Estimate:** 1d  
**Status:** todo  
**Depends on:** T039, T040, T041  
**Unlocks:** T019, T020, T021, T022, T023, T024, T025, T030  
**Human required:**   
**Acceptance criteria:** Four primitives in `src/components/primitives/`, each matching the DESIGN.md spec exactly:  
  · **FieldCard** — props: `label`, `value?`, `important?`, `listening?`, `rejecting?`. Empty state shows pulsing dot when `listening`, wheat stripe when `important`. Filled state demotes label to `MicroCaps`, value as `DisplayLead`. `rejecting` triggers a 280ms `rust` wash.  
  · **BigButton** — props: `state: 'idle' | 'pressed' | 'recording' | 'review'`, `onPress`. 168px idle, breathes via `useBreathing`. Press scales to 0.96 + medium haptic. Recording: expands to 200px, switches to `active` elevation, emits `Ripple`, screen tints to `emberWash`. Review: collapses to 80px and slides to top-right (controlled by parent layout).  
  · **PillSelector** — props: `items: { id, name, description, fieldCount, glyph? }[]`, `activeId`, `onSelect`. Active pill: `cream` fill, `lift` elevation, soft `ember` glow. Glyph slot at right, 28x28.  
  · **ToastQuiet** — props: `message`, `visible`, `onDismiss`. Pill bottom of safe area, 4px ember leading stripe, slides up `gentle` / dwells 3s / slides down `settle`.  
All four expose stable accessibility labels and roles per DESIGN.md "Accessibility" section.  
**Verification steps:** 1) Storybook-style preview screen renders all four primitives in idle, pressed, active, and disabled states. 2) BigButton state machine transitions verified manually on device. 3) Ripple effect on recording state matches design (~1.6s cadence). 4) ToastQuiet auto-dismisses after 3s. 5) VoiceOver announces "Record. Double-tap to start" on BigButton idle.  
**Verification tricky spots:** BigButton's review-state slide is a layout-controlled animation — parent must use `Layout.springify()` to handle reflow. Haptic feedback module differs between Expo SDKs (`expo-haptics`). PillSelector's "soft ember glow" is a colored shadow on iOS but a tinted border on Android (no direct equivalent).  
**Notes:** Created from DESIGN.md 2026-04-27. This task is the keystone of the visual identity — quality-gate it carefully before declaring done.  
**Assignee:**   

### T043 · Per-schema decorative SVG glyphs (wave for Fishing, sun for Art Show)

**Workstream:** WS3 — Folio  
**Criticality:** P2  
**Estimate:** 2h  
**Status:** todo  
**Depends on:** T040  
**Unlocks:**   
**Human required:**   
**Acceptance criteria:** Two single-stroke SVG components in `src/components/glyphs/`: `<WaveGlyph />` (a small horizontal wave) and `<SunGlyph />` (a soft sun with rays). Both 28x28, 1.5px stroke, soft round terminals, drawn in `inkGhost` token. Exported as React components consuming SVG props (color, size override). Used by `PillSelector` items in the schema selector (T019) — Fishing schema → wave, Art Show → sun.  
**Verification steps:** 1) Render both glyphs side-by-side at 28px — strokes and terminals look hand-drawn, not pixel-perfect geometric. 2) Color override prop works (renders in `inkMuted` for hover state). 3) Glyphs scale cleanly to 56px without losing character.  
**Verification tricky spots:** `react-native-svg` stroke rendering on Android has slight differences from iOS — check both. Avoid using `<Path>` `d` attributes that depend on rendering order.  
**Notes:** Created from DESIGN.md 2026-04-27. Decorative only — schema selector is functional without these glyphs (T019 has them as a soft dep). Future schemas would each need their own glyph.  
**Assignee:**      
